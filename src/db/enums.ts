export enum MealType {
  breakfast = 0,
  lunch = 1,
  dinner = 2,
  snack = 3,
  custom = 4,
}

export enum SyncStatus {
  pending = 0,
  synced = 1,
}

export enum FoodSource {
  manual = 0,
  barcode = 1,
  api = 2,
}
